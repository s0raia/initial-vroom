# Activated by SPRING_PROFILES_ACTIVE: docker in docker-compose.yml.
# "db" hostname resolves to the Mongo container via Docker's internal DNS.
spring.config.activate.on-profile=docker

spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://db:27017/vroom}
