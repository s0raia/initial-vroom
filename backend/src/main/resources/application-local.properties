# Activated with: mvn spring-boot:run -Dspring-boot.run.profiles=local
# Connects to a Postgres instance running directly on your machine (no Docker).
spring.config.activate.on-profile=local

spring.datasource.url=jdbc:postgresql://localhost:5432/vroom
spring.datasource.username=vroom
spring.datasource.password=vroom
