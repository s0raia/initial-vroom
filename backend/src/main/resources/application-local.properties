# Activated with: mvn spring-boot:run -Dspring-boot.run.profiles=local
# Connects to a MongoDB instance running directly on your machine (no Docker).
spring.config.activate.on-profile=local

spring.data.mongodb.uri=mongodb://localhost:27017/vroom
